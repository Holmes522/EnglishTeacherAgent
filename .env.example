# Local development only. Copy to .env; never use these credentials in deployment.
DATABASE_URL=postgresql://english_teacher:local-development-only@127.0.0.1:55432/english_teacher
REDIS_URL=redis://127.0.0.1:56379
# Explicitly enable the labelled fixture processor for the infrastructure demo.
PROCESSOR_MODE=fixture
ANONYMOUS_RETENTION_DAYS=7
